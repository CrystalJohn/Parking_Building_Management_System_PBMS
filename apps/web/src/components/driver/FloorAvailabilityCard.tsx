import { CheckCircle2, CircleAlert, Layers3 } from 'lucide-react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import type { AvailabilityItem, VehicleType } from '@/lib/driver-api'

export function FloorAvailabilityCard({ floorName, items }: { floorName: string; items: AvailabilityItem[] }) {
  return <Card className="shadow-sm"><CardHeader className="pb-3"><CardTitle className="flex items-center gap-2 text-base"><Layers3 className="size-4 text-muted-foreground" />{floorName}</CardTitle></CardHeader><CardContent className="grid gap-3 sm:grid-cols-2">{(['car', 'motorbike'] as VehicleType[]).map((type) => { const item = items.find((entry) => entry.vehicleType === type); if (!item) return null; const percentage = item.total ? Math.round((item.available / item.total) * 100) : 0; const label = item.available === 0 ? 'Full' : percentage < 35 ? 'Limited' : 'Available'; const Icon = label === 'Full' || label === 'Limited' ? CircleAlert : CheckCircle2; return <div key={type} className="rounded-xl border bg-muted/10 p-3"><div className="flex items-center justify-between gap-2"><p className="text-sm font-semibold">{type === 'car' ? 'Car' : 'Motorbike'}</p><span className="flex items-center gap-1 text-xs font-medium text-muted-foreground"><Icon className="size-3.5" />{label}</span></div><div className="mt-3 flex items-baseline justify-between"><p className="font-mono text-lg font-bold tabular-nums">{item.available}<span className="text-sm font-normal text-muted-foreground">/{item.total} available</span></p><span className="text-xs text-muted-foreground">{percentage}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className={`h-full rounded-full transition-all ${label === 'Full' ? 'bg-rose-500' : label === 'Limited' ? 'bg-amber-500' : 'bg-emerald-500'}`} style={{ width: `${percentage}%` }} /></div></div> })}</CardContent></Card>
}
